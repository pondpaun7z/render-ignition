{
  "name": "render-ignition",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "test": "node --experimental-strip-types --test src/wake.test.ts",
    "preview": "vite preview",
    "preview:pages": "pnpm build && wrangler pages dev",
    "deploy": "pnpm build && wrangler pages deploy"
  },
  "dependencies": {
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.0",
    "@vitejs/plugin-vue": "^6.0.0",
    "sass": "^1.89.0",
    "tailwindcss": "^4.1.0",
    "typescript": "^5.9.0",
    "vite": "^7.0.0",
    "vue-tsc": "^3.0.0",
    "wrangler": "^4.0.0"
  }
}
